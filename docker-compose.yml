services:
  # DuckDB is embedded (no server), so the whole demo runs in one container.
  # The DuckDB file is written under ./data on the host so it persists.
  demo:
    build: .
    command: python demo.py
    environment:
      DUCKDB_PATH: /app/data/tasks.duckdb
    volumes:
      - ./data:/app/data
